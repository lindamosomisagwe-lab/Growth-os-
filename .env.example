# Chapter Application Configuration Template

# Database Mode Selection
# Set to 'false' to connect to the real Firebase services using credentials in src/firebase-config.js.
# If set to 'true' (or omitted), the app runs in full offline mock database mode (localStorage).
# Default: true
VITE_USE_MOCK_FIREBASE=true
